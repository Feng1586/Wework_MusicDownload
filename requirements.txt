# Web Framework
fastapi==0.141.1
uvicorn[standard]==0.53.0

# XML Processing
xmltodict==1.0.4

# Caching
cachetools==7.2.0

# HTTP Requests
requests==2.34.2

# Music Download
musicdl==2.13.11

# Pydantic
pydantic==2.13.5

# Cryptography (for WeChat message encryption)
# pycryptodome 提供 Crypto.* 命名空间，企业微信 SDK 用的是它
# （注意 pycrypto 已停止维护，pycryptodome 接口完全兼容）。
pycryptodome==3.23.0

# pycryptodomex 提供 Cryptodome.* 命名空间，musicdl 的 deezer 等模块需要它。
# 两者命名空间不同，必须同时安装，不要互相替代。
pycryptodomex==3.23.0

# musicdl 自身声明 cryptography<47，这里显式钉住上界，
# 避免将来解析到 47+ 时静默破坏 musicdl 的加密相关模块。
cryptography>=46.0.5,<47
